package com.cos.blog.model;import org.hibernate.annotations.ColumnDefault;import org.hibernate.annotations.CreationTimestamp;import org.springframework.data.annotation.CreatedDate;import javax.persistence.*;import java.sql.Timestamp;@Entity // 테이블 생성 하기 위한. // User 클래스가 MySql에 테이블 생성하게 도와줌.public class User {   @Id // Primary Key   @GeneratedValue(strategy = GenerationType.IDENTITY)   //프로젝트에서 연결된 DB의 넘버링 전략을 따라간다.   //프라이머리 키를 따라감.   private int id; //시퀀스 - mysql auto_increament   // 자동으로 들어갈 것임.   @Column(nullable = false, length = 30)   private String username; // 아이디   //Column의 해당 되는 널을 사용할 수 없게 함.   @Column(nullable = false, length = 60)// => 해쉬(비밀버번호 암호화)   private String password; // 비밀번호   @Column(nullable = false, length = 50)   private String email; // 이메일   @ColumnDefault("user") // 문자라는 것을 알려줘야 한다.   private String role; // Enum을 쓰면 어떤 데이터의 도메인을 만들 수 있다.   // admin, user, manager  권한을 줄 수 있다. /도메인 : 범위를 정해져 있다. ex)  남, 녀   @CreationTimestamp// 시간이 자동으로 입력되게 함.   private Timestamp createData; //}